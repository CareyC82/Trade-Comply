name: Global Compliance Pipeline

on:
  schedule:
    # 02:00 Asia/Shanghai (UTC+8) == 18:00 UTC previous day
    - cron: "5 18 * * *"
  workflow_dispatch:
    inputs:
      offline:
        description: "Use offline fixtures only"
        required: false
        default: "false"

permissions:
  contents: write
  issues: write

concurrency:
  group: global-compliance-pipeline
  cancel-in-progress: false

jobs:
  scrape-and-stage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Python dependencies
        run: pip install -r pipeline/requirements.txt

      - name: Run global compliance pipeline
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          if [ "${{ github.event.inputs.offline }}" = "true" ]; then
            python3 pipeline/pipeline.py --offline
          else
            python3 pipeline/pipeline.py || python3 pipeline/pipeline.py --offline
          fi

      - name: Commit staged pipeline data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/pending_data/queue.json data/pending_data/pipeline_batch.json

          if git diff --staged --quiet; then
            echo "No pipeline staging changes."
            exit 0
          fi

          git commit -m "chore: stage global compliance risk signals for human review"
          git push

      - name: Create pending review GitHub Issue
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node scripts/create-pending-review-issue.js
