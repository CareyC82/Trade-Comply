/**
 * @deprecated Import from global-compliance-crawler.js instead.
 * Thin re-export for backward compatibility (admin, FC, CLI).
 */
'use strict';

module.exports = require('./global-compliance-crawler');
